{
  "name": "Python Jupyter Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.14",
  "postCreateCommand": "pip install -r requirements.txt && pip install ipykernel && python -m ipykernel install --user --name=py314 --display-name 'Python 3.14' && pip install pipreqs && pipreqs . --force",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-python.python"
      ]
    }
  }
}